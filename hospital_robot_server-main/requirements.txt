# =====================================================
# 병원용 로봇 원격 제어 웹 서버 — Python 의존성 패키지
# =====================================================
# 설치 방법:
#   pip install -r requirements.txt
#
# ROS2(rclpy, nav2_msgs, sensor_msgs 등)는 pip 으로
# 설치하지 않고 ROS2 환경(source /opt/ros/<distro>/setup.bash)에서
# 제공됩니다. ROS2 패키지는 아래 목록에서 제외합니다.
# =====================================================

# ── 웹 프레임워크 ────────────────────────────────────
Flask>=2.3.0

# ── 이미지 처리 ──────────────────────────────────────
opencv-python>=4.8.0

# ── 수치 연산 ────────────────────────────────────────
numpy>=1.24.0

# ── YOLOv8 Person Counter ────────────────────────────
ultralytics>=8.0.0

# ── MySQL 연동 ───────────────────────────────────────
PyMySQL>=1.1.0
